// particles_t contains a set of particles from the particle filter.
package mbot_lcm_msgs;

struct particles_t
{
    int64_t utime;

    int32_t num_particles;
    particle_t particles[num_particles];
}
